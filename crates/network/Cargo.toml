[package]
name = "terrarium-network"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
adapter-traits = { path = "../adapter/traits", version = "0.1.0" }
tracing = { workspace = true }
