[package]
name = "terra-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = { workspace = true }
terrarium-protocol = { path = "../protocol" }
