name: CI

on: [push, pull_request]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      # Verify Cargo.lock is up-to-date
      - run: cargo build --all --locked

      # Formatting
      - run: cargo fmt --all -- --check

      # Linting (warnings as errors)
      - run: cargo clippy --all-targets -- -D warnings

      # Tests
      - run: cargo test --all

      # Release-profile build (catches release-only failures)
      - run: cargo build --release --all

      # Security audit
      - run: cargo install cargo-audit --locked
      - run: cargo audit

      # License + dependency policy gate (cargo-deny)
      - uses: EmbarkStudios/cargo-deny-action@v2

  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Build PyO3 engine
        run: |
          pip install maturin
          cd crates/engine && maturin develop
      - run: pip install -e sdk/python/
      - run: python -c "import terra; print('SDK OK')"
      - name: Build adversarial escape probe
        run: |
          bash sdk/python/tests/adversarial/probes/build-probes.sh
          bash sdk/python/tests/adversarial/probes/build-workload.sh
      - name: Harness import/compile checks (KVM suites run on e2e-kvm)
        run: |
          python -m py_compile \
            sdk/python/tests/test_adversarial_isolation.py \
            sdk/python/tests/compare_baselines.py \
            sdk/python/tests/density_compare.py \
            sdk/python/tests/workload_overhead.py
      - name: Run non-e2e Python tests
        run: |
          pip install pytest
          python -m pytest sdk/python/tests -m "not e2e" -v
